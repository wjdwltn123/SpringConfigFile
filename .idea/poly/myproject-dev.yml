server:
  port: 12100

  management:
    endpoints:
      web:
        exposure:
          include: "*"
    endpoint:
    health:
      enabled: true
      show-details: always
    beans:
      enabled: true
    info:
      enabled: true
    sever:
      address: localhost
      port: 12101

  poly: 'BigData Dev!'
